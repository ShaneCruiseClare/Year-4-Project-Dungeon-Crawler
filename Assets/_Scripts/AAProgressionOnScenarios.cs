using UnityEngine;

namespace Project1
{
    public class AAProgressionOnScenarios
    {

        /*
         
        1. Simplest Level Scenario
            - player starts at exit location
            - exit door is unlocked

            >> Test needs to:
                - create a Maze representing this state
                - create Player, link to maze
                - create Fred/AgentPlancontrller to start running


                >> would be nice for a "planningSessionLog" to be RETURNED by Fred
                    e.g. and array of strings, which you could output to a single string,
                    which you could test against ....









        */
    
    }
}
